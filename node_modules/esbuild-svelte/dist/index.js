var __defProp = Object.defineProperty;
var __defProps = Object.defineProperties;
var __getOwnPropDescs = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols = Object.getOwnPropertySymbols;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __propIsEnum = Object.prototype.propertyIsEnumerable;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues = (a, b) => {
  for (var prop in b || (b = {}))
    if (__hasOwnProp.call(b, prop))
      __defNormalProp(a, prop, b[prop]);
  if (__getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(b)) {
      if (__propIsEnum.call(b, prop))
        __defNormalProp(a, prop, b[prop]);
    }
  return a;
};
var __spreadProps = (a, b) => __defProps(a, __getOwnPropDescs(b));
const { preprocess, compile } = require("svelte/compiler");
const { relative } = require("path");
const { promisify } = require("util");
const { readFile, statSync } = require("fs");
const convertMessage = ({ message, start, end, filename, frame }) => ({
  text: message,
  location: start && end && {
    file: filename,
    line: start.line,
    column: start.column,
    length: start.line === end.line ? end.column - start.column : 0,
    lineText: frame
  }
});
function sveltePlugin(options) {
  return {
    name: "esbuild-svelte",
    setup(build) {
      if ((options == null ? void 0 : options.cache) == void 0 && (build.initialOptions.incremental || build.initialOptions.watch)) {
        if (!options) {
          options = {};
        }
        options.cache = true;
      }
      const cssCode = new Map();
      const fileCache = new Map();
      build.onLoad({ filter: /\.svelte$/ }, async (args) => {
        var _a;
        if ((options == null ? void 0 : options.cache) === true && fileCache.has(args.path)) {
          const cachedFile = fileCache.get(args.path) || { dependencies: new Map(), data: null };
          let cacheValid = true;
          try {
            cachedFile.dependencies.forEach((time, path) => {
              if (statSync(path).mtime > time) {
                cacheValid = false;
              }
            });
          } catch {
            cacheValid = false;
          }
          if (cacheValid) {
            return cachedFile.data;
          } else {
            fileCache.delete(args.path);
          }
        }
        let source = await promisify(readFile)(args.path, "utf8");
        let filename = relative(process.cwd(), args.path);
        const dependencyModifcationTimes = new Map();
        dependencyModifcationTimes.set(args.path, statSync(args.path).mtime);
        try {
          if (options == null ? void 0 : options.preprocess) {
            let preprocessResult = await preprocess(source, options.preprocess, { filename });
            source = preprocessResult.code;
            if ((options == null ? void 0 : options.cache) === true) {
              (_a = preprocessResult.dependencies) == null ? void 0 : _a.forEach((entry) => {
                dependencyModifcationTimes.set(entry, statSync(entry).mtime);
              });
            }
          }
          let compileOptions = __spreadValues({ css: false }, options == null ? void 0 : options.compileOptions);
          let { js, css, warnings } = compile(source, __spreadProps(__spreadValues({}, compileOptions), { filename }));
          let contents = js.code + `
//# sourceMappingURL=` + js.map.toUrl();
          if (!compileOptions.css && css.code) {
            let cssPath = args.path.replace(".svelte", ".esbuild-svelte-fake-css").replace(/\\/g, "/");
            cssCode.set(cssPath, css.code + `/*# sourceMappingURL=${css.map.toUrl()}*/`);
            contents = contents + `
import "${cssPath}";`;
          }
          const result = { contents, warnings: warnings.map(convertMessage) };
          if ((options == null ? void 0 : options.cache) === true) {
            fileCache.set(args.path, { data: result, dependencies: dependencyModifcationTimes });
          }
          if (build.initialOptions.watch) {
            result.watchFiles = Array.from(dependencyModifcationTimes.keys());
          }
          return result;
        } catch (e) {
          return { errors: [convertMessage(e)] };
        }
      });
      build.onResolve({ filter: /\.esbuild-svelte-fake-css$/ }, ({ path }) => {
        return { path, namespace: "fakecss" };
      });
      build.onLoad({ filter: /\.esbuild-svelte-fake-css$/, namespace: "fakecss" }, ({ path }) => {
        const css = cssCode.get(path);
        return css ? { contents: css, loader: "css" } : null;
      });
    }
  };
}
module.exports = sveltePlugin;
